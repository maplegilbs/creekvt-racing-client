// need to build out a card for each registered racer(use same stuff in dashboard to create.), and put delete logic here. need to figure out how to pull token properly from localstorage.
const AdminRRCard = (props) => {
  return <div></div>;
};

export default AdminRRCard;
